name: RRG Daily Flight Scraper

on:
  schedule:
    # Runs every day at 04:00 AM UTC
    # - cron: '0 4 * * *'
    - cron: '0 19 * * *'
  workflow_dispatch: # Allows you to run it manually anytime from GitHub

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install seleniumbase

      - name: Run Scraper Script
        run: python scraper.py

      - name: Commit and Push Generated CSPro Files
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          git add flights_*.txt
          git commit -m "Auto-update CSPro daily flight logs [skip ci]" || echo "No changes to commit"
          git push
